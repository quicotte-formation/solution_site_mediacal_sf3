{% extends 'base.html.twig' %}

{% block body %}
    <h1>Symptome</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ symptome.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ symptome.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('symptome_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('symptome_edit', { 'id': symptome.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
